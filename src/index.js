const ENDPOINT="https://dealership.janobasson.com/api/enquiries";const json=(data,status=200)=>Response.json(data,{status,headers:{'cache-control':'no-store'}});export default{async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/api/health')return json({ok:true,service:"carvaluation.co.za",time:new Date().toISOString()});if(url.pathname==='/api/enquiries'&&request.method==='POST'){const body=await request.text();const response=await fetch(ENDPOINT,{method:'POST',headers:{'content-type':'application/json','idempotency-key':request.headers.get('idempotency-key')||crypto.randomUUID(),'x-source-site':"carvaluation.co.za"},body});return new Response(response.body,{status:response.status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}})}if(url.pathname.startsWith('/api/'))return json({error:'Not found.'},404);return env.ASSETS.fetch(request)}};